#include <stdio.h>
/**
 * main - prints the phrase "Programming is like building a multiligual puzzle
 * @myStr: This is a character variable that holds
 * the string that is to be printed out
 *
 * Return: 0 if exited properly, non-zero otherwise"
 */
int main(void)
{
	char myStr[51] = "Programming is like building a multilingual puzzle";

	puts(myStr);
	return (0);
}
