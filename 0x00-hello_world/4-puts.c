#include <stdio.h>
/**
 * main - prints the phrase
 * @myStr: string to be printed out
 *
 * Return: 0 if exited properly, non-zero otherwise
 */
int main(void)
{
	char myStr[52] = "\"Programming is like building a multilingual puzzle";

	puts(myStr);
	return (0);
}
